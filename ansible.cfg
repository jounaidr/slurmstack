[defaults]
inventory         = inventory
roles_path        = roles/
nocows            = 1
log_path          = ansible.log
remote_user       = root
host_key_checking = False
gathering         = explicit
hash_behaviour    = merge

[ssh_connection]
pipelining = True
